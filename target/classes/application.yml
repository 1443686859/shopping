mybatis:
  mapper-locations: classpath:mapping/*.xml
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: root
    username: root
    url: jdbc:mysql://127.0.0.1:3306/shopping?serverTimezone=UTC&characterEncoding=utf8&userUnicode=true&useSSL=false